const num1 = 34;
const num2 = 45;

const total = num1 + num2;
console.log(total);

const text = 'Hey my name is';
const name = 'jack';
console.log(text + ' ' +name);

// const alert = 'thanks for signing in' + ' ' +name;
// console.log(alert);

const message = `the "code" isn't working`;

const alert = `thanks for signing in ${name}`;
console.log(alert);

// length
const length = name.length;
console.log(length);

// methods

const concat = text.concat(' ',name);
console.log(concat);
const uc = text.toUpperCase();
console.log(uc);
const index = name.indexOf('c');
console.log(index);

