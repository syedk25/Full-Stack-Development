# Quick check: the final step

You just completed this chain:

```java
chatClient.prompt()
        .user(message)
        .call()
        .content();
```

What does the final `.content()` call return?
